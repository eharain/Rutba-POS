{
  "private": true,
  "workspaces": [
    "packages/*",
    "pos-desk",
    "pos-stock",
    "pos-sale",
    "pos-auth",
    "rutba-web",
    "rutba-web-user",
    "rutba-crm",
    "rutba-hr",
    "rutba-accounts",
    "rutba-payroll",
    "rutba-cms"
  ],
  "scripts": {
    "dev:desk": "node scripts/load-env.js -- npm run dev --workspace=pos-desk",
    "dev:strapi": "node scripts/load-env.js -- npm --prefix pos-strapi run develop",
    "dev:auth": "node scripts/load-env.js -- npm run dev --workspace=pos-auth",
    "dev:stock": "node scripts/load-env.js -- npm run dev --workspace=pos-stock",
    "dev:sale": "node scripts/load-env.js -- npm run dev --workspace=pos-sale",
    "dev:web": "node scripts/load-env.js -- npm run dev --workspace=rutba-web",
    "dev:web-user": "node scripts/load-env.js -- npm run dev --workspace=rutba-web-user",
    "dev:crm": "node scripts/load-env.js -- npm run dev --workspace=rutba-crm",
    "dev:hr": "node scripts/load-env.js -- npm run dev --workspace=rutba-hr",
    "dev:accounts": "node scripts/load-env.js -- npm run dev --workspace=rutba-accounts",
    "dev:payroll": "node scripts/load-env.js -- npm run dev --workspace=rutba-payroll",
    "dev:cms": "node scripts/load-env.js -- npm run dev --workspace=rutba-cms",
    "dev:all": "node scripts/run-all.js dev",
    "start:strapi": "node scripts/load-env.js -- npm --prefix pos-strapi run start",
    "start:auth": "node scripts/load-env.js -- npm run start --workspace=pos-auth",
    "start:stock": "node scripts/load-env.js -- npm run start --workspace=pos-stock",
    "start:sale": "node scripts/load-env.js -- npm run start --workspace=pos-sale",
    "start:web": "node scripts/load-env.js -- npm run start --workspace=rutba-web",
    "start:web-user": "node scripts/load-env.js -- npm run start --workspace=rutba-web-user",
    "start:crm": "node scripts/load-env.js -- npm run start --workspace=rutba-crm",
    "start:hr": "node scripts/load-env.js -- npm run start --workspace=rutba-hr",
    "start:accounts": "node scripts/load-env.js -- npm run start --workspace=rutba-accounts",
    "start:payroll": "node scripts/load-env.js -- npm run start --workspace=rutba-payroll",
    "start:cms": "node scripts/load-env.js -- npm run start --workspace=rutba-cms",
    "start:all": "node scripts/run-all.js start",
    "build:strapi": "node scripts/load-env.js -- npm --prefix pos-strapi run build",
    "build:auth": "node scripts/load-env.js -- npm run build --workspace=pos-auth",
    "build:stock": "node scripts/load-env.js -- npm run build --workspace=pos-stock",
    "build:sale": "node scripts/load-env.js -- npm run build --workspace=pos-sale",
    "build:web": "node scripts/load-env.js -- npm run build --workspace=rutba-web",
    "build:web-user": "node scripts/load-env.js -- npm run build --workspace=rutba-web-user",
    "build:crm": "node scripts/load-env.js -- npm run build --workspace=rutba-crm",
    "build:hr": "node scripts/load-env.js -- npm run build --workspace=rutba-hr",
    "build:accounts": "node scripts/load-env.js -- npm run build --workspace=rutba-accounts",
    "build:payroll": "node scripts/load-env.js -- npm run build --workspace=rutba-payroll",
    "build:cms": "node scripts/load-env.js -- npm run build --workspace=rutba-cms",
    "build:all": "npm run build:auth && npm run build:stock && npm run build:sale && npm run build:web && npm run build:web-user && npm run build:crm && npm run build:hr && npm run build:accounts && npm run build:payroll && npm run build:cms",
    "docker:env": "node scripts/generate-docker-env.js"
  },
  "dependencies": {
    "marked": "^17.0.1"
  }
}
